# Install local datavint package (now in /server)
-e .

# FastAPI and server dependencies
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12

# Core dependencies (also in pyproject.toml)
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
